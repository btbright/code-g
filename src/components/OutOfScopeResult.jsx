import React from "react";
import classnames from "classnames";

export default props => <div className={classnames("result")}>
                          <h1>This return is out of scope for VITA because the taxpayer has foreign earned income.</h1>
                        </div>
